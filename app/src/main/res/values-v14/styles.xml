<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Settings activity theme. -->
    <style name="SettingsTheme" parent="@android:style/Theme.Holo.Light.DarkActionBar">
        <item name="android:actionBarStyle">@style/ActionBar.V14.popularmovies.NoTitle</item>
    </style>

    <!-- Settings activity action bar styles -->
    <style name="ActionBar.V14.popularmovies.NoTitle" parent="@android:style/Widget.Holo.Light.ActionBar.Solid.Inverse">
        <item name="android:background">@color/toolbar_color</item>
        <item name="android:height">56dp</item>
    </style>

    <style name="ThemeToolbar" parent="Theme.AppCompat.Light">

        <!-- navigation icon color -->
        <item name="colorControlNormal">@android:color/white</item>

        <!-- color of the menu overflow icon -->
        <item name="android:textColorSecondary">@android:color/white</item>

        <item name="android:background">@color/toolbar_color</item>
        <item name="android:height">56dp</item>

    </style>
</resources>